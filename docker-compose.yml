version: "3.8"
services:
  pan-share-saver:
    build: .
    container_name: pan-share-saver
    restart: unless-stopped
    environment:
      - HEADLESS=true
      - TZ=Asia/Shanghai
      - STORAGE_DIR=/data/storage
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/data/storage
